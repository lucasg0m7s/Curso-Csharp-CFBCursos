using System;

class Aula05{
    static void Main(){
        // num++; | num += 1; | num = num + 1;
        // num *= 2; | num=num*2; + - * /
        
        // & = AND / E -- Retorna verdadeiro se todas as condições forem True
        // | = OR / OU -- Retorna falso se todas as condições forem False

        bool res=(5>3)|(10<5);

        Console.WriteLine(res);
    }
}